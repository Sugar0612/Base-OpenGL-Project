#version 330 core			
in vec4 vertexColor;
in vec2 TexPos;

uniform sampler2D aTex;
uniform sampler2D aface;

out vec4 FragColor;
void main() {				
	FragColor = mix(texture(aTex, TexPos), texture(aface, TexPos), 0.3);
}